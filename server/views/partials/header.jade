nav.navbar.navbar-inverse.navbar-fixed-top
    .container-fluid
        .navbar-header
          button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1', aria-expanded='false')
            span.sr-only Toggle navigation         
            span.icon-bar         
            span.icon-bar
            span.icon-bar
          a.navbar-brand(href='/') CodeGurukul
        #bs-example-navbar-collapse-1.collapse.navbar-collapse
          if !user
            form.navbar-form.navbar-right(action="/signup")
              button.btn.btn-primary Sign Up
            form.navbar-form.navbar-right(action="/signin" method="post")
              .form-group
                input.form-control(type="email" placeholder="Email" name="email")
              .form-group
                input.form-control(type="password" placeholder="Password" name="password")       
              button.btn.btn-primary Sign In
              a(href='/auth/facebook') Signup with Facebook
          else
              ul.navbar-right
                li 
                  h3 #{user.email}
                li
                  a(href='/signout') Signout
                li
                  img(src="#{user.profile.picture}")
                li
                  h3 #{user.profile.name}
                a(href='/dashboard') dashboard
          ul.nav.navbar-nav.navbar-left
            li
              a(href='/') Home
            if (user)
              if (user.type == "admin")
                li
                  a(href='/addcourse') Add a Course
                li
                  a(href='/viewcourses') View Courses